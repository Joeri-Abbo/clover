import React from 'react'
import {Box, Color, Text} from 'ink'
import App from './../src/components/App'

/** Command: bud {{lowercase commandName}} */
/// {{commandDescription}}
const {{CommandName}} = props => (
  <App
    budfile={join(budfileDir, '')}
    logging={props.logging}
    output={props.output}
  />
)

{{CommandName}}.propTypes = {
  /// Output directory
  output: PropTypes.string,
  /// Enable logging
  logging: PropTypes.bool,
}

{{CommandName}}.defaultProps = {
  logging: false,
}

{{CommandName}}.positionalArgs = ['output']

export default {{CommandName}}
