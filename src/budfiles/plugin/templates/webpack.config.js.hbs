const {webpack, defaults} = require('./config/build')

/**
 * Webpack configuration
 *
 * You should not need to change this in order
 * to use this framework as designed.
 *
 * Default entrypoints as follows:
 *
 *    src
 *    ├── blocks
 *    │   └── {{block-name}}
 *    │       ├── editor.css
 *    │       ├── editor.js
 *    │       ├── public.css
 *    │       └── public.js
 *    ├── plugins
 *    │   └── {{plugin-name}}
 *    │       ├── plugin.js
 *    │       └── plugin.css
 *
 * Simply replicate this structure and assets
 * should be automatically reduced out of the
 * directory structure on next build.
 */

module.exports = webpack(defaults)
