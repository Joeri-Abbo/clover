import {registerPlugin} from '@wordpress/plugins'

/**
 * Plugin: {{pluginName}}
 *
 * @param {string} name
 * @param {object} settings
 */
registerPlugin('{{dashcase (lowercase pluginName)}}', {
  render: () => {
    return null
  },
})

export default {{camelcase pluginName}}
